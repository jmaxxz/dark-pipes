describe("A truthyness", function() {
  // Let's just make sure tests run before we get all fancy like
    it("will always be", function () {
      var truth = true;
    expect(truth).toBe(true);
  });

});